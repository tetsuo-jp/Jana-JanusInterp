void shift_text(char text[4], int delta) {
    for (int i = 0; i < 3; i += 1) {
        text[i] += delta;
    }
}

void main() {
    char text[4] = "abc";
    int delta;
    delta += 1;
    call shift_text(text, delta);
    printf("%s\n", text);
}
