[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pyjanus"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
pyjanus = "jana_py.cli:main"
